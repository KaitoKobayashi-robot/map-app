rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /locations/{document=**} {
      allow read, write;
    }
    match /highlight_status/{document=**}{
      allow read;
      allow write: if false;
    }
  }
}